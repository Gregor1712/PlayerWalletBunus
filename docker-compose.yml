services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data

  api:
    build: .
    ports:  
      - "5000:5000"
    environment:
      - Redis__ConnectionString=redis:6379
    depends_on:
      - redis

volumes:
  redis-data: